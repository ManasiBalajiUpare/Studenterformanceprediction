</html>
<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Course</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">

    <style>
        body {
            background: #000;
            font-family: Arial, sans-serif;
            height: 100vh;
            margin: 0;
            display: flex;
            justify-content: center;
            /* center horizontally */
            align-items: center;
            /* center vertically */
        }
        
        .card {
            position: relative;
            max-width: 500px;
            width: 100%;
            padding: 30px;
            background: #111;
            border-radius: 15px;
            overflow: hidden;
            box-shadow: 0 0 20px rgba(255, 255, 255, 0.1);
            animation: fadeIn 1s ease forwards;
        }
        /* Animated gradient border */
        
        .card::before,
        .card::after {
            content: "";
            position: absolute;
            top: -50%;
            left: -50%;
            width: 200%;
            height: 200%;
            background: linear-gradient(60deg, #ff3cac, #784ba0, #2b86c5, #00ffff, #ff3cac);
            background-size: 400% 400%;
            animation: rotate 10s linear infinite;
            z-index: 0;
        }
        
        .card::after {
            filter: blur(20px);
        }
        
        @keyframes rotate {
            0% {
                transform: rotate(0deg);
            }
            100% {
                transform: rotate(360deg);
            }
        }
        
        @keyframes fadeIn {
            0% {
                opacity: 0;
                transform: translateY(-20px);
            }
            100% {
                opacity: 1;
                transform: translateY(0);
            }
        }
        
        .card-content {
            position: relative;
            z-index: 1;
        }
        
        h2 {
            text-align: center;
            color: #fff;
            margin-bottom: 25px;
        }
        
        label {
            color: #fff;
        }
        
        input[type="text"],
        input[type="number"],
        textarea {
            background: #111;
            border: 1px solid #fff;
            color: #fff;
            border-radius: 5px;
            padding: 10px;
            width: 100%;
            margin-bottom: 15px;
            box-sizing: border-box;
            font-size: 14px;
            transition: all 0.3s ease;
        }
        
        input:focus,
        textarea:focus {
            border-color: #ff3cac;
            box-shadow: 0 0 10px rgba(255, 60, 172, 0.5);
            outline: none;
        }
        
        .btn-primary {
            background: linear-gradient(90deg, #ff3cac, #784ba0, #2b86c5);
            border: none;
            width: 100%;
            margin-top: 10px;
            transition: all 0.3s ease;
        }
        
        .btn-primary:hover {
            transform: scale(1.05);
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.2);
        }
        
        .btn-secondary {
            width: 100%;
            margin-top: 10px;
        }
    </style>
</head>

<body>

    <%- include("navbar.ejs") %>

        <div class="card">
            <div class="card-content">
                <h2>Edit Course</h2>

                <form action="/updatecourse/<%= course.course_id %>" method="POST">
                    <label for="course_name">Course Name</label>
                    <input type="text" id="course_name" name="course_name" value="<%= course.course_name %>" required>

                    <label for="description">Course Description</label>
                    <textarea id="description" name="description" rows="3" required><%= course.description %></textarea>

                    <label for="total_credits">Total Credits</label>
                    <input type="number" id="total_credits" name="total_credits" value="<%= course.total_credits %>" required>

                    <button type="submit" class="btn btn-primary">Update Course</button>
                    <a href="/viewallcourses" class="btn btn-secondary">Cancel</a>
                </form>
            </div>
        </div>

</body>

</html>